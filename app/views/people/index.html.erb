<%= link_to "New Person", new_person_path, class: "pull-right" %>
<h1>Staff</h1>

<table class="table">
  <thead>
  <tr>
    <th>Name</th>
  </tr>
  </thead>
  <tbody>
  <% @people.each do |person| %>
    <tr>
      <td>
        <%= link_to person.title, person_path(person.id) %> <%= link_to person.first_name,person_path(person.id) %> <%= link_to person.last_name, person_path(person.id) %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
